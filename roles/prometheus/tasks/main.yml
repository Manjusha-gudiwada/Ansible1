- name: update prometheus config
  template:
    src: "{{item}}.yml"
    dest: /opt/prometheus/{{item}}.yml
  loop:
    - prometheus
    - records
    - alerts

- name: Restart prometheus service
  systemd:
    name: prometheus
    state: restarted
    enabled: yes