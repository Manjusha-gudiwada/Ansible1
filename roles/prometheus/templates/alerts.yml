groups:
- name: Hardware resources
  rules:
  - alert: HighCPU
    expr: cpu_useage_in_percentage > 90
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: HighCPU on  {%raw%}{{ $labels.tagName }}{%endraw%}